name: ci
on: [push, pull_request]
jobs:
  test:
    strategy:
      matrix:
        include:
          # x86 at the project's deployment floor (x86-64-v3: AVX2+FMA).
          # Baseline x86-64 (SSE2, no FMA) is NOT a target and rounds
          # fp-contracted expressions differently -> bitstream divergence.
          - { os: ubuntu-24.04,     cflags: "-march=x86-64-v3" }
          - { os: ubuntu-24.04-arm, cflags: "" }   # NEON baseline (Graviton-class)
          - { os: macos-15,         cflags: "" }   # Apple NEON
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: build
        run: CC=clang cmake -B build -S . -DMC_NO_NATIVE=ON -DCMAKE_C_FLAGS="${{ matrix.cflags }}" && cmake --build build -j
      - name: test
        run: ctest --test-dir build --output-on-failure
      - name: deterministic archive digest
        run: |
          clang -O3 -ffp-contract=off ${{ matrix.cflags }} -w -Isrc tests/mc_det_archive.c src/matter_compressor.c -lm -lpthread -o detarc
          mkdir -p digest && ./detarc | tee digest/${{ matrix.os }}.txt
      - uses: actions/upload-artifact@v4
        with: { name: "digest-${{ matrix.os }}", path: digest/ }
  asan:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: build+fuzz under ASan/UBSan
        run: |
          clang -O1 -g -fsanitize=address,undefined -fno-sanitize-recover=all -w -Isrc \
            tests/mc_v6_test.c src/matter_compressor.c -lm -lpthread -o v6_asan
          ./v6_asan
          clang -O1 -g -fsanitize=address,undefined -fno-sanitize-recover=all -w -Isrc \
            tests/mc_cache_test.c src/matter_compressor.c -lm -lpthread -o cache_asan
          ./cache_asan
  bitstream-identity:
    needs: test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with: { pattern: "digest-*", merge-multiple: true, path: digests }
      - name: assert byte-identical archives across ISAs
        run: |
          cat digests/*.txt
          [ "$(sort -u digests/*.txt | wc -l)" -eq 1 ] && echo IDENTICAL || { echo MISMATCH; exit 1; }
